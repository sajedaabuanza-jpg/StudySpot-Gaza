

import 'package:flutter/material.dart';
import '../details/district_home.dart';

class DeirAlBalah extends StatelessWidget {
  const DeirAlBalah({super.key});

  @override
  Widget build(BuildContext context) {
    return const DistrictHome(
      district: "دير البلح",
      title: "دير البلح",
    );
  }
}
