import 'package:flutter/material.dart';
import '../details/district_home.dart';

class AlBureij extends StatelessWidget {
  const AlBureij({super.key});

  @override
  Widget build(BuildContext context) {
    return const DistrictHome(
      district: "البريج",
      title: "البريج",
    );
  }
}