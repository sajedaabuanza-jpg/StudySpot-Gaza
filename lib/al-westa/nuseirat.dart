

import 'package:flutter/material.dart';
import '../details/district_home.dart';

class Nuseirat extends StatelessWidget {
  const Nuseirat({super.key});

  @override
  Widget build(BuildContext context) {
    return const DistrictHome(
      district: "نصيرات",
      title: "نصيرات",
    );
  }
}
