import 'package:flutter/material.dart';
import '../details/district_home.dart';

class AlMaghazi extends StatelessWidget {
  const AlMaghazi({super.key});

  @override
  Widget build(BuildContext context) {
    return const DistrictHome(
      district: "المغازي",
      title: "المغازي",
    );
  }
}