import 'package:flutter/material.dart';
import '../details/district_home.dart';

class al_nusser extends StatelessWidget {
  const al_nusser({super.key});

  @override
  Widget build(BuildContext context) {
    return const DistrictHome(
      district: "النصر",
      title: "النصر",
    );
  }
}