import 'package:flutter/material.dart';
import '../details/district_home.dart';

class alshatea extends StatelessWidget {
  const alshatea({super.key});

  @override
  Widget build(BuildContext context) {
    return const DistrictHome(
      district: "الشاطئ",
      title: "الشاطئ",
    );
  }
}