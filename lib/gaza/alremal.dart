import 'package:flutter/material.dart';
import '../details/district_home.dart';

class alremal extends StatelessWidget {
  const alremal({super.key});

  @override
  Widget build(BuildContext context) {
    return const DistrictHome(
      district: "الرمال",
      title: "الرمال",
    );
  }
}