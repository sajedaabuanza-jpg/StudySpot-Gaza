import 'package:flutter/material.dart';
import '../details/district_home.dart';

class altofah extends StatelessWidget {
  const altofah({super.key});

  @override
  Widget build(BuildContext context) {
    return const DistrictHome(
      district: "التفاح",
      title: "التفاح",
    );
  }
}