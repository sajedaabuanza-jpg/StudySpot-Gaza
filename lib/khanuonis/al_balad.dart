import 'package:flutter/material.dart';
import '../details/district_home.dart';

class al_balad extends StatelessWidget {
  const al_balad({super.key});

  @override
  Widget build(BuildContext context) {
    return const DistrictHome(
      district: "البلد",
      title: "البلد",
    );
  }
}