import 'package:flutter/material.dart';
import '../details/district_home.dart';

class al_mawasi extends StatelessWidget {
  const al_mawasi({super.key});

  @override
  Widget build(BuildContext context) {
    return const DistrictHome(
      district: "المواصي",
      title: "المواصي",
    );
  }
}